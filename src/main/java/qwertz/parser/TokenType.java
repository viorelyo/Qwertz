package qwertz.parser;

public enum  TokenType {
    NUMBER,
    WORD,

    //keywords
    PRINT,
    
    PLUS,
    MINUS,
    STAR,
    SLASH,
    EQ,
    LPAREN,     // (
    RPAREN,     // )
    EOF
}
